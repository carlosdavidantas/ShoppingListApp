const colors = {
    background: "#f8f8f8",
    primary: "#23374e",
    secondary: "#006db6",
    primaryText: "#fff",
    secoundaryText: "#acacac",
    lightText: "#e2e2e2",
    text: "#222",
    borderColor: "#e0e0e0",
    danger: "#da1600",
    success: "#0ed460cb",
    
};

const buttonsShadow = {
    shadowColor: "#000000",
    shadowOpacity: 0.3,
    shadowRadius: 4,
    elevation: 8,
    shadowOffset: { width: 0, height: 4 },
} 

const borders = {
    sm: 10,
    md: 20,
    lg: 30,
    xl: 100,
}

const spacing = {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
};

const fontSizes = {
    sm: 14,
    md: 18,
    lg: 24,
    xl: 32,
};

const fontWeights = {
    regular: "400",
    bold: "700",
};

const theme = {
    colors,
    buttonsShadow,
    borders,
    spacing,
    fontSizes,
    fontWeights,
};

export default theme;
